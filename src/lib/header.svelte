<header>
    <slot></slot>
</header>

<style>
    header {
        height: var(--headerHeight);
        background: var(--headerBackground);
        color: var(--headerColor);
        display: grid;
        place-content: center;
    }
</style>